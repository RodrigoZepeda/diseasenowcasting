.DS_Store
*.sh
*.exe
*.DS_Store
*.o
*.so
logs/
paper/
*.log
*.rds
*.pdf
# ...but DO track the precomputed datasets the vignettes load (and download as a
# fallback) so they ship with the package and the pkgdown website.
!inst/extdata/*.rds
.Rproj.user
SKILL.md
CUSTOM_COMPONENTS_PLAN.md
CENSORED_POINT*.md
SUPPLEMENT*.md
GRADIENT_STABILITY_REVIEW.md
Dockerfile

# Development scratch: analysis scripts, diagnostics and their generated
# results.  Kept on disk, never versioned -- `.Rbuildignore` already drops the
# whole directory from the package build, so nothing here ships.
devel/

# Paper drafts live next to the code but are versioned in the manuscript repo.
/*.tex
