% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_confirmation_class.R
\name{confirmation_process}
\alias{confirmation_process}
\title{Confirmation / retraction process}
\usage{
confirmation_process(retract_delay = lognormal_delay(), p = numeric(0))
}
\arguments{
\item{retract_delay}{A \code{delay_process_class} (e.g. \code{\link[=lognormal_delay]{lognormal_delay()}},
\code{\link[=gamma_delay]{gamma_delay()}}) describing the retraction delay \code{g_C} (onset of a report to
its retraction).  Default a geometric-like short lognormal.}

\item{p}{The confirmation probability (probability that a report is genuine and
never retracted), specified the same way as a delay parameter: either a
\code{prior_class} (estimated with that prior) or a single numeric in \verb{(0, 1]}
(held fixed).  \strong{Left unset (the default)}, \code{\link[=default_priors]{default_priors()}} builds a
\emph{data-informed, strongly-concentrated} Beta prior centred at the empirical
retraction rate -- exactly as the lognormal delay's \code{mu} default is
data-informed.  The strong prior is deliberate: retractions are empirically
rare (~1-2\% of reports), and with a weak prior the Skellam variance abuses the
retraction stream as an overdispersion knob and \code{p} collapses.  Pass a fixed
value (e.g. \code{p = 0.98}) to hold it constant, or your own \code{beta_prior()} to
estimate it under a prior of your choosing.}
}
\value{
A \code{confirmation_process_class} object.
}
\description{
Describes reports that are later retracted (count-cumulative streams that revise
downward as well as upward).  Attach it to a \code{\link[=model]{model()}} via the \code{confirmation}
argument; \code{\link[=nowcast]{nowcast()}} then uses the signed-increment Skellam / SkNB likelihood
when the data are count-cumulative.
}
\details{
\strong{Default priors.} \code{retract_delay} inherits the default priors of its delay
family (see \link{delay_process}); \code{p} gets a strongly-concentrated Beta centred at
the empirical retraction rate (see \code{\link[=default_priors]{default_priors()}}).  At \code{p = 1} the
confirmation layer is inert and the model is the ordinary count model.
}
\examples{
# A confirmation process with a lognormal retraction delay
confirmation_process(retract_delay = lognormal_delay())

# Attach to a model for a count-cumulative stream
model(nb_likelihood(), ar1_epidemic(), lognormal_delay(),
      confirmation = confirmation_process())

}
\seealso{
\code{\link[=model]{model()}}, \link{delay_process}, \code{\link[=nowcast]{nowcast()}}
}
