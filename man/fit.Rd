% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_fit.R
\name{fit}
\alias{fit}
\title{Fit a nowcast model with the RTMB engine}
\usage{
fit(
  model,
  data,
  priors = NULL,
  init = NULL,
  control = list(iter.max = 500, eval.max = 1000, rel.tol = 1e-09)
)
}
\arguments{
\item{model}{A \code{\link[=model]{model()}} object.}

\item{data}{Prepared-data list from \code{\link[=prepare_data]{prepare_data()}}.}

\item{priors}{Optional prior bundle; defaults to \code{\link[=default_priors]{default_priors()}}.}

\item{init}{Optional named init list.}

\item{control}{\code{nlminb} control list.}
}
\value{
A list with \code{par} (named estimates), \code{obj}, \code{opt}, \code{data}, \code{priors},
\code{model}, \code{convergence}, and (delay-only) \code{delay_mu} / \code{delay_sigma}.
}
\description{
Optimises the negative log-posterior built from \code{model} + \code{data}.  For
\code{delay_only} data this fits the reporting-delay process alone (no epidemic);
the joint epidemic fit is added in later phases.
}
