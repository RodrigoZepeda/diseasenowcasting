% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/32_prior_only.R
\name{ess}
\alias{ess}
\title{Effective sample size of the Laplace approximation (hyperparameters)}
\usage{
ess(object, n_draws = 1000L, threshold = 0.1, warn = TRUE, seed = NULL)
}
\arguments{
\item{object}{A \code{nowcast_class} (fitted, not \code{prior_only}).}

\item{n_draws}{Number of Laplace draws to reweight (default 1000).}

\item{threshold}{Warn when \code{ESS / n_draws} falls below this (default 0.10).}

\item{warn}{If \code{TRUE} (default), emit a warning when the ESS is low.}

\item{seed}{Optional RNG seed.}
}
\value{
A single numeric: the importance-sampling ESS over the
hyperparameters.  The ratio \code{ESS / n_draws} is attached as attribute
\code{"ratio"}, and the number of hyperparameters as \code{"n_hyper"}.
}
\description{
\code{diseasenowcasting} fits with a Laplace approximation: it draws \emph{independent}
samples from a Gaussian \eqn{N(\hat\theta, H^{-1})} centred at the posterior
mode, so there is no MCMC autocorrelation.  The relevant diagnostic is instead
how well that Gaussian matches the true posterior, which \code{ess()} measures with
the \strong{importance-sampling effective sample size}
\deqn{\mathrm{ESS} = \left(\sum_i w_i\right)^2 / \sum_i w_i^2, \qquad
       w_i \propto p(\theta_i \mid \text{data}) / q(\theta_i).}
}
\details{
The reweighting is done over the \strong{hyperparameters only} (the delay,
overdispersion, intercept and epidemic-kernel parameters), profiling the
high-dimensional latent field (the GP / AR coefficients) at its conditional
Laplace mode.  This is essential: a plain importance-sampling ESS over the
full parameter vector degenerates with dimension and is uninformative.  A high
ESS (close to \code{n_draws}) means the Gaussian approximation of the
hyperparameter posterior is reliable; a low ESS means it is skewed /
heavy-tailed / weakly identified -- common early in an epidemic when the data
barely move the priors.
}
\examples{
if (requireNamespace("tbl.now", quietly = TRUE)) {
  # nc <- nowcast(my_tbl_now)
  # ess(nc)
}
}
