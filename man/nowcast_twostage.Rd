% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_multisample.R
\name{nowcast_twostage}
\alias{nowcast_twostage}
\title{Two-stage multiple-imputation nowcast}
\usage{
nowcast_twostage(
  model,
  m,
  X = NULL,
  d_star = NULL,
  max_time = NULL,
  target = NULL,
  delay_window = 120L,
  K = 25L,
  floor_mu = 0.15,
  floor_sig_frac = 0.25,
  np_spread = 1,
  n_draws_per = 200L,
  phi = lognormal_prior(log(20), 0.5),
  probs = c(0.025, 0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95, 0.975),
  seed = sample.int(.Machine$integer.max, 1)
)
}
\arguments{
\item{model}{A \code{\link[=model]{model()}} object (LogNormal delay).}

\item{m}{Observation matrix \verb{[event_time, count, delay, strata...]}.}

\item{X}{Optional covariate matrix (\code{max_time} rows).}

\item{d_star}{Optional max-observable-delay vector.}

\item{max_time}{Time-window length; defaults to \code{max(m[, 1])}.}

\item{target}{Event-time to nowcast (default newest).}

\item{delay_window}{Recent window length for the Stage-1 delay fit.}

\item{K}{Number of delay imputations.}

\item{floor_mu}{Floor on the log-mean imputation SD (parametric families).}

\item{floor_sig_frac}{Floor on the delay-SD imputation SD (fraction of sigma).}

\item{np_spread}{Dirichlet only: covariance-inflation factor for the simplex
imputation (samples \code{delay_logits} from the Stage-1 Laplace posterior with
covariance scaled by \code{np_spread}).  Default 1 (the raw, well-informed
full-series posterior); values > 1 widen the simplex spread.}

\item{n_draws_per}{Posterior nowcast draws per imputation.}

\item{phi}{NB overdispersion prior (default \code{lognormal_prior(log(20), 0.5)}).}

\item{probs}{Quantile probabilities to report.}

\item{seed}{Optional RNG seed.}
}
\value{
A list with \code{quantiles}, \code{median}, pooled \code{draws}, the \code{rung} used
(\code{"multi"}, \code{"anchored"}, or \code{"onestage"}), and \code{n_samp} (imputations pooled).
}
\description{
Two-stage multiple-imputation nowcast
}
