% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/07_default_priors.R
\name{default_priors}
\alias{default_priors}
\title{Build the default prior bundle for an RTMB nowcast model}
\usage{
default_priors(mod, data = NULL, ...)
}
\arguments{
\item{mod}{A \code{\link[=model]{model()}} object.}

\item{data}{Optional prepared-data list from \code{\link[=prepare_data]{prepare_data()}} (used for the
data-informed location/scale defaults).  May also be a bare list with an
\code{m} matrix.}

\item{...}{Per-key overrides (e.g. \code{phi = lognormal_prior(log(20), 0.5)},
\code{delay_mu = normal_prior(log(5), 0.3)}).}
}
\value{
A named list of prior specs.
}
\description{
Build the default prior bundle for an RTMB nowcast model
}
\details{
The default prior for each parameter is documented on the component
constructor: see the \strong{Default priors} section of \link{epidemic_process}
(HSGP / AR(1) / SIR), \link{delay_process} (LogNormal / Gamma / GenGamma /
Dirichlet) and \link{likelihood} (the NB overdispersion \code{phi}).  To \emph{see} what a
set of priors implies for the epidemic curve before fitting, use
\link[=nowcast]{nowcast(prior_only = TRUE)}.
}
\seealso{
\link{epidemic_process}, \link{delay_process}, \link{likelihood}
}
