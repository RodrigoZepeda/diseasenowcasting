% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_epidemic_class.R
\name{custom_process}
\alias{custom_process}
\title{User-defined epidemic process}
\usage{
custom_process(
  intensity_fn,
  n_params,
  priors = list(),
  name = "Custom",
  param_names = character(0),
  inits = numeric(0)
)
}
\arguments{
\item{intensity_fn}{A function \verb{function(theta)} that takes a numeric
parameter vector and returns a numeric matrix of dimensions
\verb{[n_time x n_strata]} containing log expected incidence (the full
\code{log_mean}, not just a trend — include any intercept inside the function).
Must use only RTMB-traceable operations: \code{+}, \code{-}, \code{*}, \code{/}, \code{exp}, \code{log},
\code{sqrt}, \code{abs}, \code{sum}, \code{for} loops of \emph{fixed} length (not data-dependent).
Never use \code{if}/\code{ifelse} on parameter values, \code{pmax}/\code{pmin} on AD types, or
external solvers.  Call \code{\link[=validate_custom_process]{validate_custom_process()}} to check traceability
before fitting.}

\item{n_params}{Integer.  Total number of parameters in \code{theta}.  Must be
known at construction time; for time-varying processes (e.g. a random walk
with \code{n_time} innovations), inspect \code{prepare_data()$max_time} first to get
the correct count.}

\item{priors}{A list of length \code{n_params}.  Each element is either a
\code{prior_class} object such as \code{\link[=normal_prior]{normal_prior()}} (free parameter, estimated)
or a single numeric
scalar (fixed parameter, held constant during optimisation).
An empty list defaults all parameters to \code{std_normal_prior()}.}

\item{name}{Character label shown in print and diagnostic output.}

\item{param_names}{Character vector of length \code{n_params} with human-readable
names for the parameters.  Defaults to \code{"theta1"}, \code{"theta2"}, …}

\item{inits}{Numeric vector of length \code{n_params} with starting values.
Defaults to zero for every parameter.}
}
\value{
A \code{custom_process_class} object (subclass of
\code{epidemic_process_class}).
}
\description{
Lets you supply any RTMB-traceable function \code{intensity_fn(theta)} that
returns the log expected-incidence trajectory \verb{log_mean[n_time x n_strata]}
as the latent epidemic process for the nowcast.  This makes the framework
epidemic-process agnostic: random walks, ODE models, regression surfaces,
and anything else that can be written in AD-safe arithmetic are all valid.
}
\examples{
# Pure random walk on log-incidence (n_time = 20)
# Uses cumsum() — no [<- assignment needed, so fully AD-safe.
n_times <- 20L
rw_fn <- function(theta) {
  log_mu0  <- theta[1]
  sigma_rw <- exp(theta[2])
  eps      <- theta[3:(2L + n_times)]
  lm       <- log_mu0 + cumsum(sigma_rw * eps)
  matrix(lm, n_times, 1L)
}
proc <- custom_process(
  rw_fn,
  n_params = 2L + n_times,
  priors   = c(list(normal_prior(2, 1), normal_prior(-2, 0.5)),
               rep(list(std_normal_prior()), n_times)),
  name     = "RandomWalk",
  param_names = c("log_mu0", "log_sigma", paste0("eps_", seq_len(n_times))),
  inits    = c(2, -2, rep(0, n_times))
)
}
\seealso{
\code{\link[=validate_custom_process]{validate_custom_process()}}, \link{epidemic_process}
}
