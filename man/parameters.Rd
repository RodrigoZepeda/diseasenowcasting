% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/27_parameters.R
\name{parameters}
\alias{parameters}
\title{Parameter estimates from a fitted nowcast}
\usage{
parameters(x, conf.level = 0.95, ...)
}
\arguments{
\item{x}{A \code{nowcast_class} object.}

\item{conf.level}{Credible level for the interval (default 0.95).}

\item{...}{Unused.}
}
\value{
A \code{data.frame} with columns \code{term}, \code{estimate}, \code{std.error},
\code{conf.low}, \code{conf.high}, \code{type}.
}
\description{
Returns all estimated parameters as a long data frame with credible intervals
derived from the Laplace approximation posterior precision matrix.
}
\details{
\code{tidy()} on a nowcast gives you the \strong{nowcast} -- the predicted counts, via
\code{tbl.now}'s method for the broom generic.  This function gives you the
\strong{parameters}.  They are different questions, and this package used to answer
the second one under the first one's name.
}
\examples{
if (requireNamespace("tbl.now", quietly = TRUE)) {
  # nc <- nowcast(data, model())
  # parameters(nc)
}
}
