% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/27_tidy.R
\name{tidy.nowcast}
\alias{tidy.nowcast}
\title{Tidy a nowcast into the cross-package nowcast table}
\arguments{
\item{x}{A \code{nowcast} (from \code{\link[=nowcast]{nowcast()}}) or a \code{nowcast_prediction} (from
\code{predict()} on one).}

\item{probs}{Optional numeric probabilities in \verb{[0, 1]}.  Each adds one
column named \verb{q<probs * 100>} (so \code{0.05} gives \code{q5}, \code{0.025} gives \code{q2.5}).
Because the object keeps the draws, these quantiles are exact.}

\item{conf.level}{Width of the credible interval (default 0.95).}

\item{...}{Passed to \code{predict()} when \code{x} is a fitted nowcast (e.g.
\code{n_draws}, \code{seed}); unused otherwise.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} sorted by \code{stratum} then \code{event_date}, with
columns \code{event_date} (Date), \code{stratum} (character, \code{"all"} when the fit is
unstratified), \code{estimate} (posterior median), \code{conf.low}, \code{conf.high},
\code{level} (the width the interval actually has) and \code{engine}, plus one
column per entry of \code{probs}.
}
\description{
Returns the posterior nowcast as one row per event date per stratum, in the
column layout shared by every engine \code{tbl.now} normalises.  Called on a
fitted \code{\link[=nowcast]{nowcast()}} it first draws the posterior predictive via \code{predict()};
called on the result of \code{predict()} it summarises the draws it already holds.
}
\examples{
if (requireNamespace("tbl.now", quietly = TRUE)) {
  # nc <- nowcast(data, model(nb_likelihood(), hsgp_epidemic(), lognormal_delay()))
  # tidy(nc)                                    # one row per event date x stratum
  # tidy(predict(nc), probs = c(0.05, 0.95))    # plus exact q5 / q95 columns
  # model_parameters(nc)                        # one row per estimated parameter
}
}
\seealso{
\code{\link[=model_parameters]{model_parameters()}} for the per-parameter table that \code{tidy()}
used to return.
}
