% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_delay_class.R
\name{delay_process}
\alias{delay_process}
\alias{lognormal_delay}
\alias{gamma_delay}
\alias{generalized_gamma_delay}
\alias{dirichlet_delay}
\title{Delay distribution for the Bayesian Nowcast}
\usage{
lognormal_delay(
  mu = numeric(0),
  sigma = numeric(0),
  num_delay_seasons = 1L,
  season_distribution = std_normal_prior()
)

gamma_delay(
  shape = numeric(0),
  rate = numeric(0),
  num_delay_seasons = 1L,
  season_distribution = std_normal_prior()
)

generalized_gamma_delay(
  mu = numeric(0),
  sigma = numeric(0),
  Q = numeric(0),
  num_delay_seasons = 1L,
  season_distribution = std_normal_prior()
)

dirichlet_delay(alpha = numeric(0), bins = numeric(0))
}
\arguments{
\item{mu}{Log-mean intercept (\code{delay_mu}).}

\item{sigma}{Log-scale / SD parameter > 0.}

\item{num_delay_seasons}{Number of periodic delay seasons. Default 1.}

\item{season_distribution}{Prior for the delay-season effects.}

\item{shape, rate}{Gamma delay parameters (the \code{shape} slot is the log-mean,
the \code{rate} slot the delay SD; see the original parameterisation).}

\item{Q}{GenGamma shape (\code{delay_Q}); \code{Q = 0} recovers lognormal.}

\item{alpha}{Dirichlet concentration (scalar broadcast to all bins).}

\item{bins}{Dirichlet: number of explicit delay bins (geometric tail beyond).}
}
\value{
A \code{delay_process_class} object.
}
\description{
Specify the reporting-delay distribution.  Parameter slots accept a fixed
numeric, a \code{prior_class} (e.g. \code{\link[=normal_prior]{normal_prior()}}), or \code{numeric(0)} for the
default prior.
}
\examples{
lognormal_delay()
lognormal_delay(mu = normal_prior(log(7), 0.5))
gamma_delay()
generalized_gamma_delay(Q = 1)
dirichlet_delay(alpha = 1, bins = 21)

}
