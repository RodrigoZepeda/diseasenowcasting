% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_epidemic_class.R
\name{custom_epidemic}
\alias{custom_epidemic}
\title{User-defined epidemic process}
\usage{
custom_epidemic(
  intensity_fn,
  priors = list(),
  name = "Custom",
  param_names = character(0),
  inits = numeric(0)
)
}
\arguments{
\item{intensity_fn}{A function \verb{function(theta)} that takes a numeric
parameter vector and returns a numeric matrix of dimensions
\verb{[n_time x n_strata]} containing log expected incidence (the full
\code{log_mean}, not just a trend — include any intercept inside the function).
Must use only RTMB-traceable operations: \code{+}, \code{-}, \code{*}, \code{/}, \code{exp}, \code{log},
\code{sqrt}, \code{abs}, \code{sum}, \code{for} loops of \emph{fixed} length (not data-dependent).
Never use \code{if}/\code{ifelse} on parameter values, \code{pmax}/\code{pmin} on AD types, or
external solvers.  Call \code{\link[=validate_custom_epidemic]{validate_custom_epidemic()}} to check traceability
before fitting.}

\item{priors}{A list with one element per parameter in \code{theta}.  Each element
is either a \code{prior_class} object such as \code{\link[=normal_prior]{normal_prior()}} (free parameter,
estimated) or a single numeric scalar (fixed parameter, held constant
during optimisation).  The number of parameters is inferred from the length
of this list (or from \code{param_names} / \code{inits} if \code{priors} is omitted).  For
time-varying processes (e.g. a random walk with one innovation per
event-time), get the number of event-times with \code{\link[=infer_max_time]{infer_max_time()}} first.}

\item{name}{Character label shown in print and diagnostic output.}

\item{param_names}{Character vector naming the parameters; its length sets the
number of parameters if \code{priors} is empty.  Defaults to \code{"theta1"}, …}

\item{inits}{Numeric vector of starting values; its length sets the number of
parameters if \code{priors} and \code{param_names} are empty.  Defaults to zeros.}
}
\value{
A \code{custom_epidemic_class} object (subclass of
\code{epidemic_process_class}).
}
\description{
Lets you supply any RTMB-traceable function \code{intensity_fn(theta)} that
returns the log expected-incidence trajectory \verb{log_mean[n_time x n_strata]}
as the latent epidemic process for the nowcast.  This makes the framework
epidemic-process agnostic: random walks, ODE models, regression surfaces,
and anything else that can be written in AD-safe arithmetic are all valid.
}
\examples{
# Pure random walk on log-incidence (max_time = 20)
# Uses cumsum() — no [<- assignment needed, so fully AD-safe.
max_time <- 20L
rw_fn <- function(theta) {
  log_mu0  <- theta[1]
  sigma_rw <- exp(theta[2])
  eps      <- theta[3:(2L + max_time)]
  lm       <- log_mu0 + cumsum(sigma_rw * eps)
  matrix(lm, max_time, 1L)
}
# n_params is inferred (here 2 + max_time) from the priors list:
custom_epi <- custom_epidemic(
  rw_fn,
  priors   = c(list(normal_prior(2, 1), normal_prior(-2, 0.5)),
               rep(list(std_normal_prior()), max_time)),
  name     = "RandomWalk",
  param_names = c("log_mu0", "log_sigma", paste0("eps_", seq_len(max_time))),
  inits    = c(2, -2, rep(0, max_time))
)
}
\seealso{
\code{\link[=validate_custom_epidemic]{validate_custom_epidemic()}}, \link{epidemic_process}
}
