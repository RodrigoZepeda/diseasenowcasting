% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflows.R
\name{diseasenowcasting_workflows}
\alias{diseasenowcasting_workflows}
\title{Native and cross-engine workflows}
\description{
\code{diseasenowcasting} owns the statistical model and RTMB fit;
\link[tbl.now:tbl_nowcast]{tbl.now::tbl_nowcast} owns the common result grammar. The public result of
\code{\link[=nowcast]{nowcast()}} and \code{\link[=auto_nowcast]{auto_nowcast()}} therefore supports both layers without an
explicit conversion.
}
\section{Use the native layer for modelling}{


Use \code{\link[=model]{model()}}, \code{\link[=nowcast]{nowcast()}}, and \code{\link[=auto_nowcast]{auto_nowcast()}} when you need this package's
epidemic, delay, likelihood, revision, or cumulative-process components.
Use \code{\link[=fit_check]{fit_check()}} and \code{\link[=nowcast_diagnostic]{nowcast_diagnostic()}} for RTMB-specific convergence and
fit diagnostics. These functions automatically unwrap the retained native
fit from the common result.

\code{\link[=backtest]{backtest()}} is a native model-specification convenience, not a second
evaluation system: it translates each \code{\link[=model]{model()}} into a
\code{\link[tbl.now:engine_diseasenowcasting]{tbl.now::engine_diseasenowcasting()}} and returns
\code{\link[tbl.now:nowcast_backtest]{tbl.now::nowcast_backtest()}} directly.
}

\section{Use the common layer for results and comparison}{


Use \code{\link[tbl.now:run_nowcast]{tbl.now::run_nowcast()}} with
\code{\link[tbl.now:engine_diseasenowcasting]{tbl.now::engine_diseasenowcasting()}} when diseasenowcasting is one of several
engines. Once a fit exists, use the common methods for \code{tidy()}, \code{autoplot()},
predictive scoring, forecast conversion, ensembling, and persistence.

In particular, a backtest can be passed directly to
\code{\link[scoringutils:as_forecast_quantile]{scoringutils::as_forecast_quantile()}},
\code{\link[scoringutils:as_forecast_point]{scoringutils::as_forecast_point()}}, or, when draws were retained,
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}. Relative WIS and additional scores then
come from \code{\link[scoringutils:score]{scoringutils::score()}} and
\code{\link[scoringutils:add_relative_skill]{scoringutils::add_relative_skill()}}, rather than a package-local scorer.
}

\examples{
\dontrun{
fit <- nowcast(data, model = model())
autoplot(fit)                  # common result plot
fit_check(fit)                 # native RTMB diagnostics

bt <- backtest(data, models = list(default = model()))
relative <- bt |>
  scoringutils::as_forecast_quantile() |>
  scoringutils::score() |>
  scoringutils::add_relative_skill(metric = "wis")
}

}
