% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/22_update.R
\name{extreme_values}
\alias{extreme_values}
\title{Surprising (extreme) values flagged during the last \code{update()}}
\usage{
extreme_values(nc)
}
\arguments{
\item{nc}{A \code{nowcast_class} object returned by \code{\link[=update]{update()}}.}
}
\value{
A \code{data.frame} of flagged surprises, or \code{NULL}.
}
\description{
Returns a tidy \code{data.frame} with one row per flagged surprise -- currently the
reporting delays that arrived later than the model expects (\code{surprise = "delay"}, \code{direction = "long"}) -- together with their tail probability and
the \code{level} used.  Returns \code{NULL} when nothing was flagged (or surprise was
not computed).
}
