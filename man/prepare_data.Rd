% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/09_prepare_data.R
\name{prepare_data}
\alias{prepare_data}
\title{Prepare data for the RTMB nowcast engine}
\usage{
prepare_data(
  model,
  m,
  m_censored = NULL,
  X = NULL,
  d_star = NULL,
  delay_only = FALSE,
  max_time = NULL,
  num_strata = NULL,
  gp_L = 1.5,
  gp_boundary_frac = 0.62,
  ar_sigma_max = 1,
  is_confirmation = FALSE,
  cumulative_levels = NULL,
  cumulative_previous_nonzero = NULL,
  cumulative_settlement = NULL,
  retraction = NULL,
  resolution_mode = 0L,
  ...
)
}
\arguments{
\item{model}{A \code{\link[=model]{model()}} object.}

\item{m}{Observation matrix: columns \verb{[event_time, count, delay, strata...]},
delays 1-indexed (single stratum supported in this version).}

\item{m_censored}{Optional censored-observation matrix (same layout).}

\item{X}{Optional covariate matrix (\code{max_time} rows, P columns).}

\item{d_star}{Optional max-observable-delay vector; if NULL, computed as
\code{rev(seq_len(max_time)) - 1}.}

\item{delay_only}{If TRUE, only the delay process is prepared/fit.}

\item{max_time}{Time-window length; defaults to \code{max(m[, 1])}.}

\item{num_strata}{Number of stratum cells (the K-way product of the strata
levels). If \code{NULL}, inferred from column 4 of \code{m}. The likelihood is summed
over all \verb{max_time x num_strata} (time, stratum) cells; \code{1} is unstratified.}

\item{gp_L}{HSGP boundary factor (> 1). Default 1.5.}

\item{gp_boundary_frac}{Fraction of the HSGP domain placed left of the data.
Default 0.62.}

\item{ar_sigma_max}{Upper bound on the AR/beta RW innovation SD. Default 1.}

\item{is_confirmation}{Deprecated legacy switch.  \code{TRUE} now errors; use the
dedicated \code{cumulative} model component and cumulative arguments.}

\item{cumulative_levels}{Optional cumulative levels aligned row-for-row with
\code{m}; used only by the dedicated count-cumulative composites.}

\item{cumulative_previous_nonzero}{Optional indicators, aligned with \code{m},
that the preceding signed update was non-zero.}

\item{cumulative_settlement}{Optional positive integer settlement horizon
\code{H}. It is required for count-cumulative preparation.}

\item{retraction}{Optional list of linelist-retraction sufficient statistics
from \code{.linelist_retraction_stats()}. When supplied, the engine carries the
cure-model observation block (see 31_retraction_likelihood.R). Default NULL.}

\item{resolution_mode}{\code{0L} when the resolution observed is a RETRACTION (the
default), \code{1L} when it is a CONFIRMATION. Sets the support of the resolution
lag and what a resolved row means for the nowcast target.}

\item{...}{Reserved.}
}
\value{
A named list of engine inputs.
}
\description{
Prepare data for the RTMB nowcast engine
}
