% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/33_auto_nowcast.R
\name{auto_nowcast}
\alias{auto_nowcast}
\title{Automatically select and fit the best nowcasting model}
\usage{
auto_nowcast(
  data,
  metric = c("wis", "ape", "mse", "coverage", "coverage_50", "coverage_90"),
  type = c("auto", "two_stage", "one_stage"),
  sir = NULL,
  ar = NULL,
  hsgp = NULL,
  delays = NULL,
  likelihood = nb_likelihood(),
  models = NULL,
  n_dates = 6L,
  n_draws_select = 500L,
  n_draws = 2000L,
  K = 25L,
  min_ar = 15L,
  min_hsgp = 30L,
  now = NULL,
  seed = sample.int(.Machine$integer.max, 1),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A \code{tbl_now} object (\code{tbl.now::tbl_now()}).}

\item{metric}{Selection criterion. \code{"wis"} (default, lowest Weighted Interval
Score), \code{"ape"} (lowest absolute percentage error of the median), \code{"mse"}
(lowest mean squared error), or one of the calibration criteria, which pick
the model whose empirical interval coverage is closest to nominal:
\code{"coverage_50"} (smallest \verb{|0.50 - coverage_50|}), \code{"coverage_90"} (smallest
\verb{|0.90 - coverage_90|}), or \code{"coverage"} (smallest
\verb{|0.50 - coverage_50| + |0.90 - coverage_90|}, i.e. both intervals jointly).}

\item{type}{Stage strategy used for \emph{both} the backtest and the final fit:
\code{"auto"} (default), \code{"two_stage"}, or \code{"one_stage"} (see \code{\link[=nowcast]{nowcast()}}).}

\item{sir, ar, hsgp}{Optional epidemic-process components (e.g.
\code{sir_epidemic(R0 = ...)}) carrying your priors.  If supplied, that process is
forced into the candidate grid; otherwise the plain constructor is used when
the series length calls for it.}

\item{delays}{A list of delay components to compare.  Default:
\code{list(lognormal_delay(), generalized_gamma_delay(), dirichlet_delay())}.}

\item{likelihood}{Either a single likelihood used for every candidate
(default \code{nb_likelihood()}), or a \strong{list} of likelihoods to compare too,
e.g. \code{list(nb_likelihood(), poisson_likelihood())}.}

\item{models}{Optional \code{\link[=model]{model()}} object or list of them (e.g. carrying a
\code{\link[=custom_delay]{custom_delay()}} / \code{\link[=custom_epidemic]{custom_epidemic()}}) appended to the candidate grid so
they compete in the same backtest.}

\item{n_dates}{Number of historical dates to backtest over (default 6).}

\item{n_draws_select}{Posterior draws during the selection backtest (default
500 -- kept small for speed).}

\item{n_draws}{Posterior draws for the final fit of the winning model
(default 2000).}

\item{K}{Delay imputations for two-stage fits (default 25).}

\item{min_ar, min_hsgp}{Series-length thresholds (in event-times) at which
AR(1) and HSGP become candidates (defaults 15 and 30).}

\item{now}{As-of date for the final fit (default: the \code{tbl_now}'s \code{now}).}

\item{seed}{RNG seed.}

\item{verbose}{Print progress and the chosen model (default \code{TRUE}).}

\item{...}{Passed through to \code{\link[=backtest]{backtest()}} and \code{\link[=nowcast]{nowcast()}} (e.g.
\code{temporal_effects}).}
}
\value{
A \code{nowcast_class} (as from \code{\link[=nowcast]{nowcast()}}) for the selected model, with
the model-selection scoreboard in its \code{comparison} slot:
\code{list(scores, chosen, metric, max_time)}.
}
\description{
Takes a \code{tbl_now} and \strong{chooses a model for you}: it builds a grid of
candidate models (epidemic process x reporting-delay family) sized to how much
data you have, \code{\link[=backtest]{backtest()}}s them over several historical dates, \code{\link[=score]{score()}}s
them, keeps the best one, and refits it on the full data.  The returned object
is an ordinary \code{\link[=nowcast]{nowcast()}} result (so \code{autoplot()}, \code{predict()}, etc. work),
with the ranked scoreboard attached in its \code{comparison} slot.
}
\details{
\strong{Candidate epidemic processes are chosen by series length} (\code{max_time}, the
number of event-times): the SIR process needs the least data, the HSGP the
most.  With the default thresholds:
\itemize{
\item \code{max_time < min_ar}         -> compares \code{{SIR}};
\item \verb{min_ar <= max_time < min_hsgp} -> compares \verb{\{SIR, AR(1)\}};
\item \code{max_time >= min_hsgp}      -> compares \verb{\{AR(1), HSGP\}}.
}
Any process you pass explicitly via \code{sir} / \code{ar} / \code{hsgp} is \emph{always} included
(regardless of length), which is how you make a prior compete: e.g. pass
\code{sir = sir_epidemic(R0 = lognormal_prior(log(3), 0.2))} and the SIR candidate
will use that R0 prior throughout the comparison.

\strong{Candidate delays} default to LogNormal, Generalized-Gamma and Dirichlet;
override with \code{delays}.

\strong{Speed.} The grid is backtested with a fast configuration
(\code{n_draws_select} posterior draws over \code{n_dates} dates); only the winning model
is refit with the full \code{n_draws}.  Backtesting is the expensive step -- set a
\code{future::plan()} (e.g. \code{future::plan(multisession)}) for parallel speed-up.
}
\examples{
\donttest{
library(tbl.now)
data(denguedat)
# A short window keeps this example quick (auto_nowcast fits a whole grid):
dn <- subset(denguedat,
             onset_week >= as.Date("1990-06-01") & onset_week <= as.Date("1990-12-01"))
tn <- tbl_now(dn, event_date = onset_week, report_date = report_week,
              data_type = "linelist", verbose = FALSE)
# Backtesting the grid is the expensive step -- uncomment to run candidates
# in parallel (then restore sequential afterwards):
# future::plan(future::multisession, workers = 4)
# Compare a couple of delays; make the SIR candidate use a custom R0 prior:
nc <- auto_nowcast(tn,
                   sir    = sir_epidemic(R0 = lognormal_prior(log(2), 0.3)),
                   delays = list(lognormal_delay(), dirichlet_delay()),
                   n_dates = 2, n_draws_select = 150, n_draws = 300,
                   temporal_effects = "none")
# future::plan(future::sequential)
best_model_name(nc)    # the winning model's label
comparison_scores(nc)  # the ranked scoreboard
best_score(nc)         # just the winner's row
selection_metric(nc)   # which metric chose it
winner <- best_model(nc)  # the model() object, to reuse elsewhere
}
}
\seealso{
\code{\link[=nowcast]{nowcast()}}, \code{\link[=backtest]{backtest()}}, \code{\link[=score]{score()}}
}
