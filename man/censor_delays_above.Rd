% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/31_censoring.R
\name{censor_delays_above}
\alias{censor_delays_above}
\title{Flag reports with an implausibly long delay as censored}
\usage{
censor_delays_above(data, max_delay, quiet = FALSE)
}
\arguments{
\item{data}{A \code{tbl_now} object.}

\item{max_delay}{Numeric.  Reports with delay (report date minus event date,
in the data's event units) strictly greater than this are flagged.}

\item{quiet}{If \code{TRUE}, suppress the informational message.}
}
\value{
The \code{tbl_now} with its \code{is_censored} column updated (created if
absent).  Re-fit with \code{\link[=nowcast]{nowcast()}} to use it.
}
\description{
Marks every report whose reporting delay exceeds \code{max_delay} (in event units)
as \emph{censored}: the recorded delay becomes an upper bound rather than an exact
value.  This is the natural response to a "delay too long" surprise (see
\code{\link[=surprise]{surprise()}}): instead of letting a 300-day outlier drag the delay
distribution, you tell the model only that the case arrived \emph{by} that delay.
}
\examples{
if (requireNamespace("tbl.now", quietly = TRUE)) {
  df <- data.frame(onset = as.Date("2020-01-01") + c(0, 0, 1, 2),
                   reported = as.Date("2020-01-01") + c(1, 5, 2, 300))
  tn <- tbl.now::tbl_now(df, event_date = onset, report_date = reported,
                         data_type = "linelist", verbose = FALSE)
  tn <- censor_delays_above(tn, max_delay = 60)   # the 300-day report -> censored
}
}
