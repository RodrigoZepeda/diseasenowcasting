% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_count_cumulative_class.R
\name{count_cumulative_process}
\alias{count_cumulative_process}
\title{Count-cumulative observation process}
\usage{
count_cumulative_process(
  observation = c("hurdle_ztnb", "hurdle_ztpoisson", "cumulative"),
  retraction_delay = lognormal_delay(),
  settlement = 26L,
  retraction_mass = beta_prior(1.5, 20),
  movement_intercept = normal_prior(-1, 2),
  movement_age = normal_prior(0, 1),
  movement_previous = normal_prior(0, 1),
  magnitude_size = NULL
)
}
\arguments{
\item{observation}{Observation composite likelihood. \code{"cumulative"} uses
cumulative Poisson or negative-binomial marginals according to the model's
\link{likelihood}. \code{"hurdle_ztnb"} uses signed hurdle updates with a
zero-truncated-negative-binomial magnitude. \code{"hurdle_ztpoisson"} uses the
corresponding zero-truncated-Poisson magnitude.}

\item{retraction_delay}{Parametric delay family for retraction ages \code{1:H}.
Lognormal, gamma, and generalized gamma are supported.}

\item{settlement}{Positive integer settlement horizon \code{H}, in model steps.}

\item{retraction_mass}{Prior or fixed value in \verb{[0, 1]} for the finite-horizon
mass of \code{h_R}. A Beta prior is used by default.}

\item{movement_intercept, movement_age, movement_previous}{Priors or fixed
values for the bounded movement-probability regression. Set
\code{movement_previous = 0} to disable previous-movement dependence.}

\item{magnitude_size}{Positive prior or fixed value for the ZTNB magnitude
size. It is used only by \code{"hurdle_ztnb"}.}
}
\value{
A \code{count_cumulative_process_class} object for
\code{model(count_cumulative = )}.
}
\description{
Configures models for revision streams that publish cumulative levels. The
target is finite-horizon database retention \code{C_t(H)}, not biological truth.
The retraction mechanism is the collapsed kernel
\code{h_R(l) = retraction_mass * g_R(l)}; it does not separately identify a truth
probability and a conditional validation-delay law.
}
\examples{
count_cumulative_process()
count_cumulative_process(observation = "cumulative", settlement = 52L)
count_cumulative_process(observation = "hurdle_ztpoisson", settlement = 6L)

}
