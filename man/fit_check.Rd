% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/24_score.R
\name{fit_check}
\alias{fit_check}
\title{Check RTMB optimizer diagnostics for a fitted nowcast}
\usage{
fit_check(object, warn = TRUE)
}
\arguments{
\item{object}{A result from \code{\link[=nowcast]{nowcast()}} or \code{\link[=auto_nowcast]{auto_nowcast()}}. Native
diseasenowcasting operations unwrap the common result automatically.}

\item{warn}{If \code{TRUE}, warn when any retained fit fails the common optimizer
adequacy predicate: finite objective and derivatives, optimizer code zero,
box-constrained KKT residual, positive-definite curvature on the locally
free subspace, and a quadratic objective-gap estimate no larger than
\code{0.01}.}
}
\value{
A data frame with one row per retained RTMB fit and columns \code{fit},
\code{rung}, \code{convergence}, \code{objective}, raw and projected gradients, quadratic
objective gap, Hessian status, any Laplace-precision regularization used
for prediction, overall status, and diagnostic reasons.
}
\description{
Predictive accuracy belongs to \code{\link[tbl.now:score_nowcast]{tbl.now::score_nowcast()}} and
\code{\link[tbl.now:nowcast_backtest]{tbl.now::nowcast_backtest()}}. \code{fit_check()} deliberately reports only
diagnostics specific to the RTMB optimization performed by
diseasenowcasting.
}
\seealso{
\link{diseasenowcasting_workflows} for the distinction between native fit
diagnostics and predictive scoring; \code{\link[=nowcast_diagnostic]{nowcast_diagnostic()}},
\code{\link[tbl.now:score_nowcast]{tbl.now::score_nowcast()}}, \code{\link[tbl.now:nowcast_backtest]{tbl.now::nowcast_backtest()}}
}
