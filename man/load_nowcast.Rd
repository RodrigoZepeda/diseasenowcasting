% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/34_save_load.R
\name{load_nowcast}
\alias{load_nowcast}
\title{Load a nowcast saved with \code{\link[=save_nowcast]{save_nowcast()}}}
\usage{
load_nowcast(file, rebuild = FALSE)
}
\arguments{
\item{file}{Path to a \code{.rds} bundle written by \code{\link[=save_nowcast]{save_nowcast()}}.}

\item{rebuild}{If \code{TRUE}, also re-tape the RTMB objective for each fit (no
re-optimization).  Needed only for \code{use_random} marginal fits or to inspect
the live tape; custom delays/epidemics require \code{library(RTMB)}.  Default
\code{FALSE} -- the stored mode + precision already drive \code{\link[=predict]{predict()}}.}
}
\value{
A \code{nowcast_class} object.
}
\description{
Restores a \code{nowcast_class} from a bundle written by \code{\link[=save_nowcast]{save_nowcast()}}.  The
result works with \code{\link[=predict]{predict()}}, \code{\link[=autoplot]{autoplot()}}, \code{\link[=coef]{coef()}}, \code{\link[=tidy]{tidy()}},
\code{\link[=mean]{mean()}}/\code{\link[=median]{median()}}/\code{\link[=quantile]{quantile()}} straight away (sampling from the stored
Laplace mode + precision).  To re-fit it -- on the same or new data -- pass the
loaded object's \code{model} to \code{\link[=nowcast]{nowcast()}} (the saved \code{tbl_now} is in the \code{data}
slot).
}
\seealso{
\code{\link[=save_nowcast]{save_nowcast()}}
}
