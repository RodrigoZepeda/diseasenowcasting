% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/29_surprise.R
\name{surprise}
\alias{surprise}
\title{Compute surprise scores for new observations}
\usage{
surprise(
  object,
  new_data,
  type = c("both", "count", "delay"),
  level = 0.99,
  n_draws = 500L,
  seed = sample.int(.Machine$integer.max, 1)
)
}
\arguments{
\item{object}{A \code{nowcast_class} object (or \code{fit()} result).}

\item{new_data}{For \code{type = "count"}: a data.frame with columns
\code{event_index} (0-indexed) and \code{count} (observed new total at that time).
For \code{type = "delay"}: a data.frame with columns \code{delay} (numeric delay
values) and optionally \code{weight} (counts with that delay, default 1).
For \code{type = "both"}: supply both sets of columns.}

\item{type}{Which surprise type(s) to compute. One of \code{"count"},
\code{"delay"}, \code{"both"} (default \code{"both"}).}

\item{level}{Credible level used to flag surprises (default \code{0.99}).  A count
is flagged when it falls in the outer \code{(1 - level)/2} tail of the posterior
predictive (too high or too low); a delay is flagged when \code{P(D >= d)} or
\code{P(D <= d)} is below \code{1 - level} (surprisingly long or short).}

\item{n_draws}{Number of posterior draws (default 500).}

\item{seed}{Optional RNG seed.}
}
\value{
A list with:
\itemize{
\item \verb{$count_surprise} (data.frame): one row per event time with
\code{event_index}, \code{observed}, \code{posterior_mean}, \code{lpd}, \code{ppp_right},
\code{ppp_left}, \code{relative_surprise}.
\item \verb{$delay_surprise} (data.frame): one row per delay value with
\code{delay}, \code{tail_prob} (= 1 - G_D(delay)), \code{lpd}, \code{relative_surprise}.
}
}
\description{
Given a fitted nowcast and new data that arrived \emph{after} the fit, compute
how surprising the new data is under the fitted posterior.

Two types of surprise are supported (select via the \code{type} argument):
\itemize{
\item \code{"count"}: How surprising is a new case count at a given event time
\item \code{"delay"}: How surprising is a report with a very long delay
\item \code{"both"}: Compute both (requires appropriate \code{new_data} format).
}
}
