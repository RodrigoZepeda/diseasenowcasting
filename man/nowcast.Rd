% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/15_nowcast.R
\name{nowcast}
\alias{nowcast}
\title{Posterior-predictive nowcast draws (full curve) from a joint RTMB fit}
\usage{
nowcast(
  fit,
  target = NULL,
  n_draws = 1000L,
  probs = c(0.025, 0.05, 0.1, 0.25, 0.5, 0.75, 0.9, 0.95, 0.975),
  seed = NULL
)
}
\arguments{
\item{fit}{A joint fit from \code{\link[=fit]{fit()}} (non-\code{delay_only}).}

\item{target}{Optional event index for the convenience \code{quantiles}/\code{median}
summary (default newest).}

\item{n_draws}{Number of posterior parameter draws.}

\item{probs}{Quantile probabilities for the convenience summary.}

\item{seed}{Optional RNG seed.}
}
\value{
list(\code{M} = draws matrix, \code{nowcast} = \code{\link[=summarise_nowcast_matrix]{summarise_nowcast_matrix()}}
table, \code{draws}/\code{quantiles}/\code{median}/\code{observed} at \code{target}).
}
\description{
Samples the joint posterior (fixed + random) from the Laplace approximation
and draws the complete count at EVERY event time, returning a
\verb{[n_draws x max_time]} matrix (events ascending).  This is the dcast3
analogue of the Stan generated-quantities \code{nowcast[t]}.
}
