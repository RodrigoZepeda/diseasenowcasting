% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/20_nowcast_class.R
\name{nowcast}
\alias{nowcast}
\title{Fit a nowcast model to censored reporting data}
\usage{
nowcast(
  data,
  model = model(),
  type = c("two_stage", "one_stage"),
  now = NULL,
  K = 25L,
  n_draws = 2000L,
  delay_window = 120L,
  np_spread = 1,
  floor_mu = 0.15,
  floor_sig_frac = 0.25,
  temporal_effects = "auto",
  seed = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{tbl_now} object (\code{tbl.now::tbl_now()}).}

\item{model}{A \code{\link[=model]{model()}} object.  Default: \code{model()} (NB + HSGP + Dirichlet).}

\item{type}{\code{"two_stage"} (default; delay-imputation pooling) or \code{"one_stage"}
(a single joint fit).}

\item{now}{As-of date; only events/reports up to \code{now} are used.  Default:
\code{tbl.now::get_now(data)}, falling back to the latest report date.}

\item{K}{Number of delay imputations for the two-stage path.}

\item{n_draws}{Default number of posterior draws used by \code{\link[=predict]{predict()}} and the
latent-incidence summaries.}

\item{delay_window}{Recent window length for the parametric Stage-1 delay fit.}

\item{np_spread}{Dirichlet simplex imputation covariance inflation (default 1).}

\item{floor_mu, floor_sig_frac}{Imputation-spread floors (parametric families).}

\item{temporal_effects}{Controls automatic seasonal / day-of-week covariates.
\code{"auto"} (default) adds sensible effects based on the data's time unit
(weekly -> 52-period seasonality; daily -> day-of-week + 52-period
seasonality; monthly -> 12-period seasonality) \strong{only if the \code{tbl_now}
does not already carry computed temporal effects}.  Use \code{"none"} (or
\code{"None"}) to disable, or pre-attach your own effects to the \code{tbl_now} with
\code{tbl.now::add_temporal_effects()} + \code{tbl.now::compute_temporal_effects()}.}

\item{seed}{Optional RNG seed (imputation draws).}

\item{...}{Passed to \code{\link[=prepare_data]{prepare_data()}} (e.g. \code{gp_boundary_frac}).}
}
\value{
A \code{nowcast_class} object.
}
\description{
The main entry point.  Takes a \code{tbl_now} (from the tbl.now package) and a
\code{\link[=model]{model()}}, fits the latent-epidemic + reporting-delay model as of a given
date, and returns a \code{nowcast_class} object.  Fitting only -- the
posterior-predictive nowcast is produced lazily by \code{\link[=predict]{predict()}}; the latent
incidence by \code{\link[=mean]{mean()}}/\code{\link[=median]{median()}}/\code{\link[=quantile]{quantile()}}; the parameter estimates by
\code{\link[=coef]{coef()}}.
}
\section{Overdispersion (\code{phi})}{

The negative-binomial overdispersion prior is \strong{not} an argument of
\code{nowcast()}.  Set it on the likelihood instead, e.g.
\code{model(nb_likelihood(phi = lognormal_prior(log(5), 0.5)), ...)}.  The default
\code{nb_likelihood()} already uses \code{lognormal_prior(log(20), 0.5)}.
}

\examples{
if (requireNamespace("tbl.now", quietly = TRUE)) {
  # data <- tbl.now::tbl_now(my_linelist, event_date = onset, report_date = reported)
  # nc <- nowcast(data, model(nb_likelihood(), hsgp_epidemic(), lognormal_delay()))
  # predict(nc); median(nc); coef(nc)
}
}
