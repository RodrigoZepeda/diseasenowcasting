% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/24_score.R
\name{score}
\alias{score}
\title{Score a backtest: WIS, APE, MSE per model (and rank them)}
\usage{
score(object, metric = c("wis", "ape", "mse"), report = TRUE)
}
\arguments{
\item{object}{A \code{backtest_class}.}

\item{metric}{Metric to RANK models by: \code{"wis"} (default), \code{"ape"}, or \code{"mse"}.
All three are always reported; \code{metric} only chooses the ranking.}

\item{report}{If TRUE (default), print the ranked comparison via cli.}
}
\value{
A data.frame, one row per model, with \code{wis}, its decomposition
(\code{overprediction}, \code{underprediction}, \code{dispersion}), \code{ape}, \code{mse},
\code{coverage_50}, and \code{coverage_90}, sorted best-first by \code{metric}.
}
\description{
Score a backtest: WIS, APE, MSE per model (and rank them)
}
